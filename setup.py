from setuptools import setup

setup(name='gym_sip',
      version='0.1.0',
      install_requires=['gym', 'pandas', 'scikit-learn', 'matplotlib', 
      					'torch', 'torchvision']  # And any other dependencies foo needs
)  
